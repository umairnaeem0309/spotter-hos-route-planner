# Frontend environment template. Copy to frontend/.env.local for local dev.
# Only VITE_* values are exposed to the browser; never put secrets here.

# Base URL of the Django API (include the /api suffix).
VITE_API_BASE_URL=http://localhost:8000/api
